export function convertionWeight(weight) {
    const weightM = parseFloat(weight / 10).toFixed(2);
    return weightM;
}

export function convertionHeight(height) {
    const heightM = parseFloat(height / 10).toFixed(2);
    return heightM;
}